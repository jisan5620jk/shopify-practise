<!-- Drawer -->
<div
  id="drawer"
  class="fixed inset-y-0 left-0 w-64 bg-white shadow-lg transform -translate-x-0 transition-transform duration-300 ease-in-out z-30"
>
  <div class="flex items-center justify-between px-4 py-3 border-b">
    <h2 class="text-lg font-semibold text-gray-700">Drawer</h2>
    <button
      id="closeDrawerBtn"
      aria-label="Close drawer"
      class="md:hidden text-gray-600 hover:text-gray-900 focus:outline-none focus:ring-2 focus:ring-indigo-500 rounded"
    >
      ✕
    </button>
  </div>
  <div class="p-4 text-gray-700 text-lg">Cart Items</div>
</div>

<!-- Overlay -->
<div id="overlay" class="fixed inset-0 bg-black bg-opacity-30 hidden md:hidden z-20"></div>
